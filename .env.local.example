# Generate with: openssl rand -hex 32
AUTH0_SECRET=
APP_BASE_URL=http://localhost:3000
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# Machine-to-Machine app for Management API (email → org membership lookup)
AUTH0_MGMT_CLIENT_ID=
AUTH0_MGMT_CLIENT_SECRET=

# Optional: subdomain auto-login (acme.localhost:3000 → workspace acme)
# ORG_SLUG_MAP={"acme":"org_REPLACE_ACME"}
# APP_ROOT_HOST=localhost:3000

# Optional: API audience for access tokens (future)
# AUTH0_AUDIENCE=
